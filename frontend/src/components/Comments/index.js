import React from 'react';
import { useContext } from 'react';
import { prodcutContext } from '../Products';


const Comment = () => {
  
    return (
        <div>
            <p>{}</p>
        </div>
    );
}

export default Comment;
